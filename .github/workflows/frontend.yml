on:
  push:
    branches:
    - main

jobs:
  frontend:
    name: Build frontend
    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@v4

      - name: Build
        run: cd frontend && docker build -t frontend:${{ github.sha }} Containerfile 
